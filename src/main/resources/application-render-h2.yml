spring:
  datasource:
    url: jdbc:h2:file:./pomodoridb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  sql:
    init:
      mode: always
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console

# ---------------------------------------------------------
# CONFIGURAZIONE POSTGRES (COMMENTATA PER IL FUTURO)
# ---------------------------------------------------------
# spring:
#   config:
#     activate:
#       on-profile: prod-postgres
#   datasource:
#     url: ${DB_URL}
#     username: ${DB_USER}
#     password: ${DB_PASS}
#     driver-class-name: org.postgresql.Driver
#   jpa:
#     hibernate:
#       ddl-auto: update
#     properties:
#       hibernate:
#         dialect: org.hibernate.dialect.PostgreSQLDialect
