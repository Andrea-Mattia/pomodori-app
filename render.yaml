services:
  - type: web
    name: pomodori-app
    env: java
    buildCommand: ./mvnw clean install
    startCommand: java -jar target/*.jar

databases:
  - name: pomodori-db
    databaseName: pomodori
    user: pomodori_user
